export enum PriceRange {
  MIN_VALUE = 0,
  MAX_VALUE = 1000000,
}

export enum MileageRange {
  MIN_VALUE = 0,
  MAX_VALUE = 1000000,
}

export enum YearRange {
  MIN_VALUE = 1930,
  MAX_VALUE = 2023,
}
